---
name: Test workflow
on:
  push:
  workflow_dispatch:

jobs:
  peepoo:
    name: "PeePoo"
    # runs-on: debian-latest
    runs-on: self-hosted
    steps:
      - name: Run test command
        run: echo "hello world"
